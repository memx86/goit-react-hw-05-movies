// import PropTypes from "prop-types";

import Container from "components/Container";
import Section from "components/Section";

function MovieCard(props) {
  return (
    <Section>
      <Container>MovieCard</Container>
    </Section>
  );
}

// MovieCard.propTypes = {};

export default MovieCard;
